# 短信转发程序

运行在中移物联网 ML307A模组 OpenCPU 环境下的应用：接收模组当前 手机 卡
收到的短信，解析出收件时间、发件号码与正文，通过 HTTP 上报到指定服务器。
(只能用移动手机卡)

## 编译与烧录

本程序只包含应用层代码，编译需要中移物联网 OpenCPU SDK 提供的内核库、工具链与打包
脚本。把本目录放到 SDK 的 `custom/applaction/` 下，按 SDK 自带的编译脚本编译，再用
官方工具下载固件：

```
ML307A_build.bat GCLN      
```

SDK 需自行从中移物联网官方渠道获取



